version: '3'

services:
  zoo-app:
    container_name: zoo-app
    tty: true
    stdin_open: true
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - .:/app
      - zoo-app-bundle-cache:/usr/local/bundle
